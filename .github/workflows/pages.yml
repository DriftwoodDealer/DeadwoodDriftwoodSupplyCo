name: Deprecated - now Vercel

on:
  push:
    branches:
      - dev
  workflow_dispatch:

# Deprecated - now Vercel. The workflow is intentionally disabled so GitHub Pages
# cannot become a competing production deployment.

jobs:
  deprecated:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deprecated - now Vercel"
